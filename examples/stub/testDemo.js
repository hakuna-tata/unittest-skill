const globalApp = {
    window: {
        larkFun: {
            invoke: function(params1, params, parmas3, cb) {
                return cb();
            }
        }
    }
}

module.exports = {
    globalApp
};